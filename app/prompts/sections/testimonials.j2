{% include "_shared.j2" %}

SECTION 4 - TESTIMONIALS
========================
Role: Social-proof feel without rendering fake quote cards. Show the
product loved by real-feeling customers.

Scene:
  - Background is an ambient lifestyle scene (light cafe table, warm
    interior, soft sunlight).
  - The product (ref #1) tastefully placed off-center, accompanied by
    everyday objects: a journal, a small plant, a folded soft fabric.
  - Layout includes {{ copy.testimonials.items|length }} stacked
    semi-transparent panels for the testimonial quotes.

Arabic text to render on the image (RTL, correctly connected letterforms):
  - HEADLINE (top, large, bold):
    {{ copy.testimonials.headline|jsafe }}
  - TESTIMONIAL CARDS (each inside its own panel, with the quote first
    in italic, the name and location below in smaller weight):
{% for t in copy.testimonials.items %}    {{ loop.index }}. QUOTE: {{ t.quote|jsafe }}
       NAME: {{ t.name|jsafe }}
       LOCATION: {{ t.location|jsafe }}
{% endfor %}