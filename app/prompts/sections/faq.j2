{% include "_shared.j2" %}

SECTION 5 - FAQ
===============
Role: An informative calm panel that visually reads as 'questions answered'.

Scene:
  - Reading-room mood: soft beige and warm gradient background.
  - The product (ref #1) sits in the lower third, small but recognizable.
  - Upper two thirds: open, uncluttered area with subtle illustrative
    motifs (soft circles, dotted lines, gentle paper texture). No literal
    speech bubbles.
  - Layout reserves {{ copy.faq.items|length }} stacked semi-transparent
    panels for the Q and A pairs.

Arabic text to render on the image (RTL, correctly connected letterforms):
  - HEADLINE (top, large, bold):
    {{ copy.faq.headline|jsafe }}
  - Q AND A LIST (each pair inside its own panel, the question prefixed
    with the Arabic letter for question, the answer prefixed with the
    Arabic letter for answer):
{% for q in copy.faq.items %}    {{ loop.index }}. QUESTION: {{ q.question|jsafe }}
       ANSWER: {{ q.answer|jsafe }}
{% endfor %}