{% include "_shared.j2" %}

SECTION 3 — BEFORE / AFTER
==========================
Role: A tasteful, non-medical visual comparison.

Scene:
  - Top half: a "before" mood representing "{{ copy.before_after.before }}".
    Cooler / muted color treatment, no negative imagery, suggested through
    color and texture only.
  - Bottom half: an "after" mood representing "{{ copy.before_after.after }}".
    Warmer, brighter, with the product (ref #1) clearly visible as the
    transformative element.
  - Divider: a soft visual transition (gradient band, particles, or curve)
    — NEVER a hard horizontal split line.
  - Reserve a calm zone near the middle for an overlay headline.

Mood cue:
  - Before: {{ copy.before_after.before }}
  - After: {{ copy.before_after.after }}
