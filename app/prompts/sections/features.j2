{% include "_shared.j2" %}

SECTION 2 — FEATURES
====================
Role: Showcase up to {{ copy.features.items|length }} core features of the
product through visual vignettes around it.

Scene:
  - The product (ref #1) anchored at center.
  - Around it, {{ copy.features.items|length }} small contextual vignettes
    or icon-like illustrations representing each feature.
  - Clean premium catalog vibe, soft drop shadows, breathing room.
  - Reserve vertical bands to the side of each vignette where a short
    feature label can be overlaid as text afterward.

Feature labels (DO NOT draw text — leave clear, calm zones for them):
{% for f in copy.features.items %}  - {{ loop.index }}. {{ f.title }} — {{ f.description }}
{% endfor %}
