{% include "_shared.j2" %}

SECTION 2 - FEATURES
====================
Role: Showcase {{ copy.features.items|length }} core features through
visual vignettes around the product.

Scene:
  - The product (ref #1) anchored at center.
  - {{ copy.features.items|length }} small contextual vignettes or
    icon-style illustrations arranged around it, each representing one
    feature.
  - Clean premium catalog vibe, soft drop shadows, breathing room.
  - Each vignette has a calm panel beside it for its label.

Arabic text to render on the image (RTL, correctly connected letterforms):
  - HEADLINE (top, large, bold):
    {{ copy.features.headline|jsafe }}
  - FEATURE LABELS (each next to its matching vignette, medium weight,
    title bigger than description):
{% for f in copy.features.items %}    {{ loop.index }}. TITLE: {{ f.title|jsafe }}
       DESCRIPTION: {{ f.description|jsafe }}
{% endfor %}
