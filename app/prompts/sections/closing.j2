{% include "_shared.j2" %}

SECTION 8 — CLOSING / CTA
=========================
Role: A confident closing visual that drives the final purchase.

Scene:
  - The product (ref #1) center-frame again, slightly different angle
    from the hero — gift-worthy, premium, almost ceremonial.
  - Subtle radiant background — soft beams of warm light, gradient
    glow behind the product.
  - Lower third: a calm dark or saturated band ready to host the final
    CTA overlay.
  - Optional supporting props: a soft fabric, a folded ribbon, small
    botanical sprigs — sparse, premium.

Copy overlay zones (DO NOT draw text — leave the regions clear):
  - Upper third: headline "{{ copy.closing.headline }}"
  - Middle: body "{{ copy.closing.body }}"
  - Lower band: CTA "{{ copy.closing.cta }}"
